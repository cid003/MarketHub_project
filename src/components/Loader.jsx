import React from 'react';
import './Loader.css';

const Loader = (props) => {
    return (
        <div className="loader-div">
            <h3>Loading....</h3>
        </div>
    );
};

export default Loader;